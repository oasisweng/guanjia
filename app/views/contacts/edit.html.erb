<% provide(:title, '编辑地址') %>
<h1>编辑地址</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@contact) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>
      <%= f.label :full_name, "姓名" %>
      <%= f.text_field :full_name, class: 'form-control' %>
      <br>
      <%= f.label :male, "称呼"%>
      <%= f.radio_button :male, 1, checked: @contact.male, class: 'form-control' %>
      <%= f.label :male, "先生", :value => true %>
      <%= f.radio_button :male, 0, checked: !@contact.male, class: 'form-control' %>
      <%= f.label :male, "女士", :value => false %>
      <br>
      <%= f.label :phone_no, "电话号码" %>
      <%= f.text_field :phone_no, class: 'form-control' %>
      <br>
      <%= f.label :address, "地址" %>
      <%= f.text_field :address, class: 'form-control' %>
      <br>
      <%= f.submit "Save contact", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
